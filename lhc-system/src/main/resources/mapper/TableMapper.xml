<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lhc.newV.system.mapper.TableMapper">
    <select id="findList" resultType="com.lhc.newV.system.entity.vo.TableColumnVO" parameterType="com.lhc.newV.system.entity.vo.TableColumnVO">
        SELECT
            a.id tableId,
            a.NAME tableName,
            a.description tableDescription,
            a.alias tableAlias,
            b.id columnId,
            b.name columnName,
            b.alias columnAlias,
            b.description columnDescription,
            b.data_type,
            b.is_primary_key,
            b.foreign_key_id,
            b.foreign_table_id
        FROM
        `n_table` a
        JOIN n_column b ON a.id = b.table_id
        <where>
            <if test="tableName != ''">
                and a.NAME like '%${tableName}%'
            </if>
            <if test="foreignKeyName!=''">
                and b.name like '%${foreignKeyName}%'
            </if>
        </where>
    </select>
</mapper>